// Package pubsub реализует in-memory шину публикации/подписки с обобщённым
// типом сообщения. PubSub потокобезопасен, поддерживает fan-out на несколько
// подписчиков, drop-политику для медленных получателей и идемпотентное
// корректное завершение через Shutdown.
package pubsub
